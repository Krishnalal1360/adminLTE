@extends('admin.layouts.master')

@section('content')
<div class="content-wrapper p-4">
    {{--  
    <h1 class="mb-4">Welcome, {{ auth()->user()->name }}</h1>

    <div class="card">
        <div class="card-body">
            <p>You are now logged in as <strong>{{ auth()->user()->email }}</strong>.</p>
        </div>
    </div>
    --}}
</div>
@endsection
